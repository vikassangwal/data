version: 1.0
runtime: python311
build:
  commands:
    build:
      - cd backend
      - pip install -r requirements.txt
run:
  command: cd backend && uvicorn main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
