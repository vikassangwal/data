import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Services | Alex Kumar',
  description: 'Premium data analytics, AI agent services, chatbot development, web automation, and custom software solutions starting at competitive rates.',
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
